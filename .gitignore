# Built Singularity images (rebuild with setup/build.sh)
builds/*.sif
builds/.build_staging/
setup/.build_staging/

# Python
__pycache__/
*.py[cod]

# Local agent notes, not published
/AGENTS.md
/CLAUDE.md
# Project config created at runtime (anchored to root — tests/assets/ ships
# a template of this same filename that must stay tracked)
/.agentic_peer_project.json
/.agentic_peer_project.json.bak
